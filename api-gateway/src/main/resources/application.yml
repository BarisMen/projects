server:
  port: 1111
spring:
  application.name: gateway-client
  cloud:
    gateway:
      routes:
        - id: movies-route
          uri: http://localhost:9999/movies
          predicates:
            Path=/movies/**
        - id: actor-route
          uri: http://localhost:9998/acors
          predicates:
            Path=/actors/**